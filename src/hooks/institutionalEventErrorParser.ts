import { ErrorParser } from './errorParser';

class InstitutionalEventErrorParser extends ErrorParser {
  constructor() {
    super('Evento Institucional');
  }
}

export default InstitutionalEventErrorParser;
