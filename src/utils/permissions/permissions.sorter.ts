import { PermissionResponse } from '../../models/http/responses/permissions.response.models';

export function sortPermissionResponse(
  A: PermissionResponse,
  B: PermissionResponse,
) {
  return 0;
}
